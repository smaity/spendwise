# Local secrets / credentials — never commit
SpendWise/Secrets.swift

# Local deploy settings (team ID / device UUID) — never commit
deploy.env

# Build artifacts
build/
DerivedData/
*.xcarchive
*.ipa

# Xcode user state
*.xcuserstate
xcuserdata/

# macOS
.DS_Store
