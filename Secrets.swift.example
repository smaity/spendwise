// Template for SpendWise local secrets.
// Copy this file to `SpendWise/Secrets.swift` and fill in your own values.
// `SpendWise/Secrets.swift` is git-ignored and must NOT be committed.

import Foundation

enum AppSecrets {
    /// Google OAuth iOS client ID from Google Cloud Console (Application type: iOS).
    /// PKCE flow → no client secret is needed.
    static let googleClientID = "YOUR_IOS_CLIENT_ID.apps.googleusercontent.com"
}
